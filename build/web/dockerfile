FROM node:12-alpine AS build_web
RUN apk add go build-base git
RUN git clone https://github.com/lanyulei/ferry_web.git
RUN cd ferry_web && npm install && npm run build:prod

FROM alpine
COPY --from=build_web /ferry_web/web /web
CMD [ "bash" ]